# devConnect

## TUTORIAL 
1. `git clone <URL>`
2. `git checkout -b <Branch>`
3. **Kerjakan todo kalian ?**

## Setelah selesai mengerjakan 
1. `git add .` atau `git add <NAMA_FILE>`
2. `git commit -m "<MESSAGE>"`
3. `git push origin "<UR Branch>"` 